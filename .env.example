# Backend API Configuration
# The base URL for your backend API
NEXT_PUBLIC_API_URL=http://localhost:8080

# PDF Service Configuration (Optional)
# The base URL for the PDF service (if used separately)
NEXT_PUBLIC_PDF_SERVICE_URL=http://localhost:3001

# Next.js Configuration
# Set to 'production' for production builds
NODE_ENV=development

# Optional: Razorpay Configuration (if payment integration is client-side)
# NEXT_PUBLIC_RAZORPAY_KEY_ID=your_razorpay_key_id

# Optional: Application Base URL (for metadata and canonical URLs)
# NEXT_PUBLIC_APP_URL=http://localhost:3000